unit uCadCarro;

interface

uses
  Winapi.Windows, Winapi.Messages, System.SysUtils, System.Variants, System.Classes, Vcl.Graphics,
  Vcl.Controls, Vcl.Forms, Vcl.Dialogs, Vcl.ExtCtrls, Vcl.StdCtrls;

type
  TfCadCarro = class(TForm)
    Panel1: TPanel;
    Label1: TLabel;
    eCarro: TEdit;
    Label2: TLabel;
    ePlaca: TEdit;
    Button1: TButton;
    ComboBox1: TComboBox;
    procedure Button1Click(Sender: TObject);
  private
    { Private declarations }
  public
    { Public declarations }
  end;

var
  fCadCarro: TfCadCarro;

implementation

{$R *.dfm}

procedure TfCadCarro.Button1Click(Sender: TObject);
begin
  ShowMessage('Carro cadastrado! Nome: ' + eCarro.Text + ' Placa: ' + ePlaca.Text);
end;

end.
