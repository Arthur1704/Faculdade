program pLocadora;

uses
  Vcl.Forms,
  uMenu in '..\uMenu.pas' {frmMenu},
  fmrCliente in 'fmrCliente.pas' {Form1};

{$R *.res}

begin
  Application.Initialize;
  Application.MainFormOnTaskbar := True;
  Application.CreateForm(TfrmMenu, frmMenu);
  Application.CreateForm(TForm1, Form1);
  Application.Run;
end.
